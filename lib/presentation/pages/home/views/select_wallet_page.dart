import 'package:flutter/material.dart';

class SelectWalletPage extends StatelessWidget {
  const SelectWalletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Select Wallet'),
      ),
      body: Center(
        child: Text('Select Wallet Page'),
      ),
    );
  }
}